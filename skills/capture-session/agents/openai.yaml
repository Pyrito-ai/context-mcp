interface:
  display_name: "Capture Session"
  short_description: "Save useful session context to MemoryCore"
  default_prompt: "Use $capture-session to save the useful outcomes from this session."
policy:
  allow_implicit_invocation: false
